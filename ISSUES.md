# مشکلات شناسایی شده در ریپوزیتوری

این سند تمامی مشکلات امنیتی، کیفیت کد و بهترین شیوه‌های برنامه‌نویسی که در این پروژه شناسایی شده‌اند را فهرست می‌کند.

## 🔴 مشکلات امنیتی حیاتی (Critical Security Issues)

### 1. آسیب‌پذیری SQL Injection در financial.php
**شدت:** بسیار بالا  
**مکان:** `pages/financial.php` - خطوط 31، 38، 51، 64

**توضیح:**  
پارامترهای `$start_date` و `$end_date` که مستقیماً از `$_GET` دریافت می‌شوند، بدون هیچ اعتبارسنجی یا Sanitize شدن در کوئری‌های SQL استفاده شده‌اند.

**کد مشکل‌دار:**
```php
$start_date = $_GET['start_date'] ?? date('Y-m-01');
$end_date = $_GET['end_date'] ?? date('Y-m-d');
// ...
$income_query = "SELECT SUM(amount) as total_income FROM transactions 
                 WHERE type IN ('admin_add', 'bot_add') 
                 AND created_at BETWEEN '$start_date' AND '$end_date 23:59:59'";
```

**خطر:**  
مهاجم می‌تواند با ارسال مقادیر مخرب در پارامترهای GET، کوئری‌های SQL دلخواه را اجرا کرده و به اطلاعات حساس دسترسی پیدا کند یا پایگاه داده را تغییر دهد.

**راه حل:**  
استفاده از Prepared Statements یا اعتبارسنجی دقیق تاریخ‌ها با regex.

---

### 2. افشای اطلاعات حساس در کد (Hardcoded Credentials)
**شدت:** بسیار بالا  
**مکان:** `pages/tel/utils/db.php` - خط 6

**توضیح:**  
رمز عبور پایگاه داده به صورت Hardcode در کد قرار گرفته است.

**کد مشکل‌دار:**
```php
$password = 'Hamed@141512';
```

**خطر:**  
- اطلاعات ورود به پایگاه داده در کد منبع قابل مشاهده است
- هر کسی که به مخزن دسترسی داشته باشد می‌تواند رمز عبور را ببیند
- رمز عبور در تاریخچه Git ذخیره می‌شود

**راه حل:**  
- استفاده از فایل‌های پیکربندی خارج از Git (مانند .env)
- اضافه کردن فایل‌های حاوی اطلاعات حساس به .gitignore
- استفاده از متغیرهای محیطی (Environment Variables)

---

### 3. توکن پیش‌فرض ربات تلگرام
**شدت:** متوسط  
**مکان:** `pages/config.php` - خط 3

**توضیح:**  
توکن ربات تلگرام به صورت مقدار پیش‌فرض `'bottoken'` تنظیم شده که می‌تواند منجر به مشکلات امنیتی شود.

**کد مشکل‌دار:**
```php
define('TOKEN', 'bottoken');
```

**خطر:**  
اگر توسط کاربران نهایی تغییر نشود، ربات کار نمی‌کند و در صورت کامیت شدن توکن واقعی، اطلاعات حساس افشا می‌شود.

---

## 🟡 مشکلات کیفیت کد (Code Quality Issues)

### 4. عدم وجود فایل .gitignore
**شدت:** متوسط  
**مکان:** ریشه پروژه

**توضیح:**  
پروژه فاقد فایل `.gitignore` است که می‌تواند منجر به کامیت شدن فایل‌های غیرضروری یا حساس شود.

**فایل‌هایی که باید Ignore شوند:**
- فایل‌های پیکربندی محلی (config.local.php)
- فایل‌های لاگ (*.log)
- فایل‌های موقت (*.tmp)
- پوشه‌های وندور (vendor/)
- فایل‌های session

---

### 5. استفاده ناهماهنگ از PDO و MySQLi
**شدت:** پایین  
**مکان:** در سراسر پروژه

**توضیح:**  
پروژه از هر دو روش PDO و MySQLi برای اتصال به پایگاه داده استفاده می‌کند که باعث ناهماهنگی و پیچیدگی می‌شود.

**نمونه‌ها:**
- `config/db.php`: استفاده از PDO
- `pages/financial.php`: استفاده از MySQLi
- `pages/broadcast.php`: استفاده از PDO
- `pages/tel/utils/db.php`: استفاده از MySQLi

**پیشنهاد:**  
استفاده یکپارچه از PDO در تمام پروژه (PDO انعطاف‌پذیرتر و امن‌تر است)

---

### 6. تکرار کد در ایجاد اتصال پایگاه داده
**شدت:** پایین  
**مکان:** فایل‌های متعدد در پوشه pages/

**توضیح:**  
کد ایجاد اتصال به پایگاه داده در هر فایل تکرار شده است:

```php
$conn = new mysqli(DB_HOST, DB_USER, DB_PASS, DB_NAME);
if ($conn->connect_error) {
    die("خطا در اتصال به دیتابیس: " . $conn->connect_error);
}
```

**پیشنهاد:**  
ایجاد یک فایل مرکزی برای مدیریت اتصال پایگاه داده و استفاده مجدد از آن.

---

### 7. عدم اعتبارسنجی ورودی در broadcast.php
**شدت:** متوسط  
**مکان:** `pages/broadcast.php`

**توضیح:**  
- عدم محدودیت اندازه پیام
- عدم بررسی نوع و اندازه فایل تصویر
- عدم Rate Limiting برای جلوگیری از ارسال پیام‌های اسپم

**خطر:**  
امکان سوءاستفاده از سیستم ارسال پیام سراسری برای اسپم یا آپلود فایل‌های مخرب.

---

### 8. استفاده از include_path نسبی
**شدت:** پایین  
**مکان:** `pages/broadcast.php` - خط 4

**توضیح:**  
کامنت در کد نشان‌دهنده عدم اطمینان از مسیر صحیح فایل config.php است:

```php
require_once __DIR__ . '/config.php'; // اگر config.php در همان پوشه pages است
```

**مشکل:**  
این می‌تواند منجر به خطاهای include شود اگر ساختار پوشه تغییر کند.

---

## 🔵 بهبودهای پیشنهادی (Recommended Improvements)

### 9. عدم مدیریت خطا در broadcast.php
**توضیح:**  
در صورت خطا در ارسال پیام به برخی کاربران، فرآیند ادامه می‌یابد اما خطاها لاگ نمی‌شوند.

**پیشنهاد:**  
- لاگ کردن خطاها برای بررسی بعدی
- نمایش لیست کاربرانی که پیام به آن‌ها ارسال نشد

---

### 10. نبود سیستم لاگ یکپارچه
**توضیح:**  
برخی قسمت‌ها از Logger استفاده می‌کنند (مانند getdatapanel.php) اما بسیاری از فایل‌ها فاقد سیستم لاگ هستند.

**پیشنهاد:**  
استفاده از یک سیستم لاگ یکپارچه در تمام پروژه.

---

### 11. عدم وجود مستندات API
**توضیح:**  
فایل‌هایی مانند `getdatapanel.php` که به عنوان API عمل می‌کنند، فاقد مستندات هستند.

**پیشنهاد:**  
افزودن مستندات برای هر endpoint شامل:
- پارامترهای ورودی
- فرمت پاسخ
- کدهای خطا

---

### 12. نبود Validation برای پارامترهای ورودی
**مکان:** فایل‌های متعدد

**توضیح:**  
بسیاری از پارامترهای دریافتی از `$_GET` و `$_POST` بدون اعتبارسنجی کافی استفاده می‌شوند.

**پیشنهاد:**  
- استفاده از توابع فیلتر PHP (filter_var)
- اعتبارسنجی نوع داده
- محدودیت طول رشته‌ها

---

## خلاصه اولویت‌ها

### فوری (Critical):
1. ✅ رفع SQL Injection در financial.php
2. ✅ حذف اطلاعات حساس از کد
3. ✅ افزودن .gitignore

### مهم (High):
4. اعتبارسنجی ورودی‌ها
5. یکپارچه‌سازی روش اتصال به پایگاه داده
6. افزودن سیستم لاگ یکپارچه

### پیشنهادی (Medium):
7. بهبود مدیریت خطا
8. افزودن مستندات
9. Refactor کردن کدهای تکراری

---

**تاریخ تهیه:** 2024
**نسخه:** 1.0
